/*
 * Please do not edit this file.
 * It was generated using rpcgen.
 */

#include "kv_store.h"
#include <stdio.h>
#include <stdlib.h>
#include <rpc/pmap_clnt.h>
#include <string.h>
#include <memory.h>
#include <sys/socket.h>
#include <netinet/in.h>

#ifndef SIG_PF
#define SIG_PF void(*)(int)
#endif

void
kvstore_1(struct svc_req *rqstp, register SVCXPRT *transp)
{
	union {
		int example_1_arg;
		buf echo_1_arg;
		kv put_1_arg;
		buf get_1_arg;
	} argument;
	char *result;
	xdrproc_t _xdr_argument, _xdr_result;
	char *(*local)(char *, struct svc_req *);

	switch (rqstp->rq_proc) {
	case NULLPROC:
		(void) svc_sendreply (transp, (xdrproc_t) xdr_void, (char *)NULL);
		return;

	case EXAMPLE:
		_xdr_argument = (xdrproc_t) xdr_int;
		_xdr_result = (xdrproc_t) xdr_int;
		local = (char *(*)(char *, struct svc_req *)) example_1_svc;
		break;

	case ECHO:
		_xdr_argument = (xdrproc_t) xdr_buf;
		_xdr_result = (xdrproc_t) xdr_buf;
		local = (char *(*)(char *, struct svc_req *)) echo_1_svc;
		break;

	case PUT:
		_xdr_argument = (xdrproc_t) xdr_kv;
		_xdr_result = (xdrproc_t) xdr_void;
		local = (char *(*)(char *, struct svc_req *)) put_1_svc;
		break;

	case GET:
		_xdr_argument = (xdrproc_t) xdr_buf;
		_xdr_result = (xdrproc_t) xdr_buf;
		local = (char *(*)(char *, struct svc_req *)) get_1_svc;
		break;

	default:
		svcerr_noproc (transp);
		return;
	}
	memset ((char *)&argument, 0, sizeof (argument));
	if (!svc_getargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		svcerr_decode (transp);
		return;
	}
	result = (*local)((char *)&argument, rqstp);
	if (result != NULL && !svc_sendreply(transp, (xdrproc_t) _xdr_result, result)) {
		svcerr_systemerr (transp);
	}
	if (!svc_freeargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		fprintf (stderr, "%s", "unable to free arguments");
		exit (1);
	}
	return;
}
